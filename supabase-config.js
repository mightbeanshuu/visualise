/* ════════════════════════════════════════════════════════════════
   SUPABASE CONFIG  —  fill these in with YOUR project's values.
   Find them in: Supabase dashboard → Project Settings → API.

   • url      = "Project URL"        (e.g. https://abcxyz.supabase.co)
   • anonKey  = "anon / public" key  (the PUBLIC one, NOT service_role)

   The anon key is meant to be public and is safe to commit — your data
   is protected by Row-Level Security (see SUPABASE_SETUP.md).

   ⚠️ Until you replace the YOUR-... placeholders below, the site runs in
      "demo mode": the login gate is DISABLED so nothing is locked out.
   ════════════════════════════════════════════════════════════════ */
window.SUPABASE_CONFIG = {
  url:     "https://YOUR-PROJECT-ref.supabase.co",
  anonKey: "YOUR-PUBLIC-ANON-KEY"
};
